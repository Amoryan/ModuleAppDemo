ext {
    cfg = [
            "applicationId"    : "com.fxyan.moduleapp",

            "compileSdkVersion": 28,
            "minSdkVersion"    : 18,
            "targetSdkVersion" : 28,

            "versionCode"      : 1,
            "versionName"      : "1.0.0",
    ]

    versions = [
            "arouter-compiler": "1.2.2",
    ]
}